%h1 Login
= form_tag sessions_url, method: :post do
  - if @user.errors.any?
    #error_explanation
      %h2
        = pluralize(@user.errors.count, "error")
        prohibited this address from being saved:
      %ul
        - @user.errors.full_messages.each do |message|
          %li= message
  = text_field_tag :email, @email, placeholder: 'Email'
  %br
  .actions
    = button_tag "Send Login Email"